{
  "private": true,
  "dependencies": {
    "basic-auth": "^2.0.1",
    "do-not-cache": "^1.0.1",
    "pino": "^6.2.1",
    "pino-http": "^5.1.0",
    "url-parse": "^1.4.7"
  },
  "devDependencies": {
    "depcheck": "^0.9.2",
    "husky": "^4.2.5",
    "licensee": "^8.1.0",
    "nyc": "^15.0.1",
    "simple-concat": "^1.0.0",
    "standard": "^14.3.3",
    "tape": "^5.0.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run precommit"
    }
  },
  "scripts": {
    "precommit": "npm run lint && npm run coverage && npm run licenses && npm run deps",
    "coverage": "nyc npm run test",
    "deps": "depcheck",
    "fix": "standard --fix",
    "licenses": "licensee --errors-only",
    "lint": "standard",
    "test": "node test.js"
  }
}
